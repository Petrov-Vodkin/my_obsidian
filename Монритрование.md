2021-02-17 15:29
#монритрование
[link](https://losst.ru/montirovanie-diska-v-linux)
# Монритрование
```bash
fdisk -l	# увидеть все подключенные к системе устройства
lsblk 		# именена устройств для чтения компакт дисков

mount file_that_mount dir_where_mount   # коротко обычно в /mnt
mount опции -t файловая_система -o опции_монтирования файл_устройства папка_назначения
mount		# список всех примонтированных устройств	
# монтирование через UUID
sudu blkid	# посмотреть uuid для разделов 
sudo mount --uuid="UUID" /mnt
# монтирование через МЕТКИ
sudo mount --label="home" /mnt/
# Монтировать одну ПАПКА в другую
`sudo mount --bind /mnt/ /media/
# Монтирование ОБРАЗ ДИСКА ISO 
sudo mount ~/file.iso /mnt
```
#### Размонтировать
```bash
sudo umount /mnt	# размонтировать у-во в рапке mnt
lsof -w /mnt 		# программы, использующие файл в этой папке (/mnt)
umount -l /mnt		# файловая система будет отключена немедленно(даже если используется)

```

```bash
"ОПЦИИ"
-V 		    # вывести версию утилиты;
-h 		    # вывести справку;
-v 		    # подробный режим;
-a, --all 	# примонтировать все устройства, описанные в fstab;
-F, --fork 	# создавать отдельный экземпляр mount для каждого отдельного раздела;
-f, --fake  	  # не выполнять никаких действий, а только посмотреть что собирается делать утилита;
-n, --no-mtab 	  # не записывать данные о монтировании в /etc/mtab;
-l, --show-labels # добавить метку диска к точке монтирования;
-c				  # использовать только абсолютные пути;
-r, --read-only   # монтировать раздел только для чтения;
-w, --rw 	  	  # монтировать для чтения и записи;
-L, --label 	  # монтировать раздел по метке;
-U, --uuid 	  	  # монтировать раздел по UUID;
-T, --fstab 	  # использовать альтернативный fstab;
-B, --bind 	  	  # монтировать локальную папку;
-R, --rbind 	  # перемонтировать локальную папку.
```
_____________
#### Links
[[Linux]] [[Debian]] [[Основные консольные команды Linux (Debian)]]